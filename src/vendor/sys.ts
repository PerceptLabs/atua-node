/**
 * Node.js sys module — alias for util.
 *
 * This is the deprecated `require('sys')` which is just `require('util')`.
 */
export const __atua = true;

export * from './util.js';
export { default } from './util.js';
